// python3.13
// cargo
// npm
// pip
// python3.13-venv

> python3.13 -m venv venv
> source venv/bin/activate
> pip install 
> cargo build --release
> cd source-code && cd gui && npm install && npm run build
